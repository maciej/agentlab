# AgentLab Smoke Sandbox

This directory is safe fixture data for testing AgentLab read-only tools.

The sandbox includes a tiny service note, a task list, and a log sample. A model
can list files, search for text, and read files here without seeing the host
filesystem.

Smoke test target:

- codename: aurora
- owner: AgentLab
- expected status: ready
